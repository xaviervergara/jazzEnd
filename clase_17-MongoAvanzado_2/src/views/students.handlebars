<div>
  <h1>Estudiantes</h1>
  <ul>
    {{#each data.docs}}
      <li>{{this.first_name}} {{this.last_name}}</li>

    {{/each}}
  </ul>
  <br />
  <br />
  {{#if data.hasPrevPage}}
    <button><a href={{data.prevLink}}>Pagina anterior</a></button>
  {{/if}}
  {{#if data.hasNextPage}}
    <button><a href={{data.nextLink}}>Pagina siguiente</a></button>
  {{/if}}
</div>

{{! data.docs (recordemos que paginate nos devuelve el objeto que incluye docs y ahi dentro estan los documentos!) }}